#include "hashData.h"

HashData *createHashData (char *word, int key) {
  HashData *h = malloc(sizeof(HashData));
  printf("%s $$$$ %d\n", word, key);
  h->word = word;
  h->key = key;
  printf("%s !!!! %d\n", h->word, h->key);
  return h;
}
