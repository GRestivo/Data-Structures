#include "listData.h"

HashData *createHashData (List *list, int key) {
  HashData *d = malloc(sizeof(HashData));
  d->list = list;
  d->key = key;
  return d;
}

sHashData *createSHashData (sList *slist) {
  sHashData *s = malloc(sizeof(sHashData));
  s->slist = slist;
  return s;
}
