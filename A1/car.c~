#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "car.h"

Car *createCar( char direction, char turn, int time )
{
    Car *c = malloc(sizeof(Car));
    c->direction  = direction;
    c->turn = turn;
    c->timeArrived = time;
    return c;
}
/*
char *getDirection( Car *c )
{
    return c->direction;
}

char *getTurn( Car *c )
{
    return c->turn;
}

int getTimeArrived ( Car *c )
{
  return c->timeArrived;
}

void deleteCar(void *toDelete)
{
  free(toDelete);
}

void setCar(Car *c, char *direction, char *turn, int time)
{
  c->direction = direction;
  c->turn = turn;
  c->timeArrived = time;
}

void printCar( void *c )
{
   Car *cP = (Car*)c;
   printf("%c %c %d\n", *getDirection(cP), *getTurn(cP), getTimeArrived(cP));
}

*/
